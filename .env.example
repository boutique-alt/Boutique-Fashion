# Razorpay (public key only — secret stays on backend)
VITE_RAZORPAY_KEY_ID=

# Admin dashboard (temporary — replace with Supabase auth later)
VITE_ADMIN_PASSWORD=admin123

# Supabase (wire up later)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
